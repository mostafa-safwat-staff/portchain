# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build portcalls-analyzer-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t portcalls-analyzer-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system portcalls-analyzer-api && \
          adduser --system -G portcalls-analyzer-api portcalls-analyzer-api

COPY dist/apps/portcalls-analyzer-api portcalls-analyzer-api/
RUN chown -R portcalls-analyzer-api:portcalls-analyzer-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix portcalls-analyzer-api --omit=dev -f install

CMD [ "node", "portcalls-analyzer-api" ]
